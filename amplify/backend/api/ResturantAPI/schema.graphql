type Restaurant @model @auth(
  rules: [
  { allow: groups, groups: ["Admin"] },
  { allow: owner, groups: ["ContentCreator"] },
  {allow: private, operations: [read]}
  {allow: public, operations: [read]}
  ]
){
  id: ID!
  name: String!
  description: String!
  city: String!
}